// Coda Parser Demo 3
// This program is syntactically correct,
// and it contains multiple levels of nested
// note blocks that the parser correctly encodes.

// Piano section of New York, I Love You, But You're Bringing Me Down
!key[F-]
!sig[6, 8]
!tmp[165]

rep[4]{
    typ[8]{
        F+4*
    }

    typ[4]{
        D-5*
    }

    typ[4]{
        C#-4*
    }
}
